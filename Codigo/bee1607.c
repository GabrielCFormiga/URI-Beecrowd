#include <stdio.h>
#include <string.h>

int main(){
    char strA[10001], strB[10001];
    int testes, op, t;

    scanf("%d", &testes);

    while(testes--){
        scanf("%s %s", strA, strB);
        t = strlen(strA);

        op = 0;
        
        for(int i = 0; i < t; t++){
            
        }


    }

    return 0;
}